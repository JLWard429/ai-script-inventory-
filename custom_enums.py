import enum

# Re-export standard library enums for convenience
Enum = enum.Enum
IntEnum = enum.IntEnum
Flag = enum.Flag
IntFlag = enum.IntFlag
auto = enum.auto

# Example: add your own custom enums if needed
# class MyCustomEnum(enum.Enum):
#     FOO = 1
#     BAR = 2
